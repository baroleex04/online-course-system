<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Past Courses</title>
</head>
<body>
    <h1>Past Courses</h1>
    <% if (courses.length > 0) { %>
        <ul>
            <% courses.forEach(course => { %>
                <li>
                    <h3><%= course.name %> (<%= course.cid %>)</h3>
                    <p>Credits: <%= course.credit %></p>
                    <p>Semester: <%= course.semester %> 
                       (Year: 20<%= course.semester.slice(0, 2) %>, Semester: <%= course.semester.slice(2) %>)</p>
                    <p>Instructor: <%= course.instructor %></p>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>You have not taken any courses in the past.</p>
    <% } %>
</body>
</html>
