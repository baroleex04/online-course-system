<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
<style>
    .content {
        position: relative;
        height: 100vh; /* Full viewport height */
    }
    .content img {
        width: 100%;
        height: 100%;
        object-fit: cover; /* Ensures the image covers the entire section */
        position: absolute;
        top: 0;
        left: 0;
        z-index: -1; /* Places the image behind the text */
    }
    .content .welcome-text {
        position: relative;
        z-index: 1; /* Ensures the text is above the image */
        padding-top: 100px; /* Moves the welcome message down */
    }
    body {
        display: flex;
    }
    .sidebar {
        min-height: 100vh;
        width: 250px;
        background-color: #343a40;
        color: white;
        position: fixed;
    }
    .sidebar .nav-link {
        display: block;
        color: #adb5bd;
    }
    .sidebar .nav-link:hover {
        color: white;
        background-color: #495057;
    }
    .content {
        margin-left: 250px;
        padding: 20px;
        width: 100%;
    }
</style>